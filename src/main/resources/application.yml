spring:
  application:
    name: cuoco
  datasource:
    url: jdbc:mysql://localhost:3306/cuoco?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
jwt:
  secret: defaultsecretkeyforjwtauth123456
